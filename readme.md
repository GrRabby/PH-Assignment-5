1. What is the difference between var, let, and const?
=> Var is function scope where let and const are block scope, var can be redeclare and reassigned where let can be reassign and const dosen't
2. What is the spread operator (...)?
=> (...) extends the arrays elements into individual elements
3. What is the difference between map(), filter(), and forEach()?
=> map() returns each elements, filter() returns element based on condition and forEach() returns undefined
4.What is an arrow function?
=> shorter way to write a function in js
5. What are template literals?
=> This is a feature that allows write multi line string and variable insertion.